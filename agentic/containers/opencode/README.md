# sst/opencode agent image (Task 4.3) — scaffold

Derive from `../mcp/mcp.sif`, wire MCP HTTP JSON-RPC at `localhost:8080`, vLLM
via `NO_PROXY` host, GWDG proxy for web egress (Task 2.5).

**Status:** scaffold only — implementation follows `containers/openhands/` pattern.
